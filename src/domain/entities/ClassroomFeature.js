class ClassroomFeature {
    constructor({ id, name }) {
        this.id = id; // ID of the classroom feature
        this.name = name; // Name of the classroom feature (e.g., projector, whiteboard, air conditioning)
    }
}

export default ClassroomFeature;