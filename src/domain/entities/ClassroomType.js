class ClassroomType {
    constructor({ id, name }) {
        this.id = id; // ID of the classroom type
        this.name = name; // Name of the classroom type (e.g., laboratory, classroom, auditorium)
    }
}