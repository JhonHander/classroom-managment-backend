class Sensor {
    constructor({ id, classroomId, status }) {
        this.id = id; // ID of the sensor
        this.classroomId = classroomId; // ID of the classroom associated with the sensor
        this.status = status; // Status of the sensor (e.g., active, inactive)
    }
}