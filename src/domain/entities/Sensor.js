class Sensor {
    constructor({ id, classroom, status }) {
        this.id = id; // ID of the sensor
        this.classroom = classroom; // ID of the classroom associated with the sensor
        this.status = status; // Status of the sensor (e.g., active, inactive)
    }
}